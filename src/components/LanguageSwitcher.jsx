import React from 'react';
import GoogleTranslate from './GoogleTranslate';

/** Automatic page translation (Google) + English i18n source strings. */
export default function LanguageSwitcher() {
  return <GoogleTranslate />;
}
